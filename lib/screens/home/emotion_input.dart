
import 'package:flutter/material.dart';

class EmotionInput extends StatelessWidget {
  const EmotionInput({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Card(child: Text("some emojis and shit")),
    );
  }
}